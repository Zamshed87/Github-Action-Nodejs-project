import { CSSProperties } from "react";

export interface FlexProps {
  justify?: string;
  align?: string;
  direction?: string;
  wrap?: string;
  gap?: string;
  className?: string;
  style?: CSSProperties;
}
